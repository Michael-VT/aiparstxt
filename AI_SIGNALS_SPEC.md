# AI Signal Specification (v0.4.0)

Каноническое описание сигналов и скоринга AI-детекции для всех реализаций
(`parscgpt-ext.py` — референс; `partxt-ext` в Python/Rust/Go/C++/Node/Bun —
порты, обязанные воспроизводить ту же формулу и те же базы). Порты должны
давать одинаковый `AI Probability` ±2 п.п. на одном файле.

Калибровка: 34 подтверждённых AI-ответа (`aitext/`) против 22 human-текстов
Wikisource (`tmp/validation_corpus/`), см. `validation/AI_CORPUS_REPORT.md`.
Результат на корпусе калибровки (IN-SAMPLE, не оценка обобщения): порог 50 →
recall 32/34, FP 0/21; порог 55 → 30/34; порог 70 → 20/34. Leave-one-out по
фразовым уровням результаты не меняет. ВАЖНО: пороги CV и уровни фраз
подбирались на этом же корпусе — на текстах других жанров, современных
human-текстах и коротких фрагментах показатели будут ниже; тестовые тексты
корпуса предназначены для исследования, а не как образец «истины».

## 1. Структурные сигналы (основные)

### 1.1 sentence_length_cv (CV длины предложений)

```
sentences = split(text по [.!?]+, предложения > 3 символов)
lens      = [кол-во слов предложения]
sent_cv   = pstdev(lens) / mean(lens)          # 0, если предложений < 2
```

Корпус: AI 0.27–0.44; human 0.42–1.53.

Начисление (плавное масштабирование надёжности, v0.4.1; жёсткий порог шума
`n_sentences >= 5` И `total_words >= 40`, ниже — 0):

| sent_cv < | баллы |
|---|---|
| 0.30 | 32 |
| 0.35 | 26 |
| 0.40 | 19 |
| 0.45 | 11 |
| 0.50 | 5 |

### 1.2 paragraph_length_cv (CV длины абзацев)

```
paragraphs = split(text по \n\s*\n, абзацы > 15 слов)
para_cv    = pstdev(lens) / mean(lens)          # не определён при < 4 абзацев
```

Корпус: AI 0.06–0.40; human 0.45–0.98.

| para_cv < | баллы |
|---|---|
| 0.15 | 28 |
| 0.25 | 22 |
| 0.35 | 16 |
| 0.45 | 7 |

Масштабирование: баллы умножаются на `min(1, n_sentences/15) × min(1, words/150)`
(предложения) и `min(1, n_paragraphs/4)` (абзацы, вычисляется от ≥3 абзацев);
joint — на минимум двух масштабов. Короткие тексты получают частичные баллы
вместо молчаливого нуля; при `words < 150` или `sentences < 5` вердикт
дополняется пометкой о ненадёжности (см. раздел 5).

### 1.3 joint_uniformity (оба CV низкие)

Корпус: 33/34 AI имеют оба CV < 0.45; 0/10 human (где para_cv определён).
Требует, чтобы sentence_cv уже начислил баллы (гарды 1.1 выполнены) и
para_cv определён.

| sent_cv < | para_cv < | баллы |
|---|---|---|
| 0.40 | 0.40 | 14 |
| 0.45 | 0.45 | 10 |

## 2. Фразовые базы (4 языка, merged-поиск по подстроке в lower-case)

Уровни назначены по human-корпусу: HIGH — 0 попаданий у human; MEDIUM —
редкие; WEAK — встречаются и у людей (evidence-only, почти без веса).
Канонические списки — в `parscgpt-ext.py`, `AI_PHRASES` (EN/RU/UK/PT в
каждом уровне). Кратко по составу (~150 фраз):

- **high** (~75): "it is important to note", "plays a crucial role",
  "a wide range of", "стоит отметить", "играет важную роль",
  "является одним из", "варто зазначити", "відіграє важливу роль",
  "vale ressaltar", "desempenha um papel", "cada vez mais", …
- **medium** (~45): "moreover", "furthermore", "более того",
  "с одной стороны", "крім того", "по-перше", "além disso",
  "dessa forma", "um dos mais", …
- **weak** (~45): "however", "various", "таким образом", "при этом",
  "однако", "таким чином", "зокрема", "no entanto", "portanto", …

Подсчёт: число вхождений (всех) каждой фразы по каждому уровню →
`tiers = {high: N, medium: N, weak: N}`.

| условие | баллы |
|---|---|
| high ≥ 2 | 24 |
| high = 1 | 15 |
| medium ≥ 3 | 10 |
| medium ≥ 1 | 5 |
| weak ≥ 4 | 4 |

### 2.1 Переиспользование шаблонных заголовков (v0.4.2)

Строчный аналог pattern_repetition: дословно повторяющиеся короткие строки-
заголовки — признак структурированных LLM-ответов («Что верно» ×7, «Итог» ×7).
На human-корпусе — 0 срабатываний. Добавлено по внешнему held-out случаю
(структурированный разбор видео), не по корпусу калибровки.

```
кандидат: строка 4–60 символов, 1–8 слов, без конечной пунктуации
          (.!?:;,…"»„), не начинается с цифры
total    = сумма повторов строк с count >= 3
distinct = число таких строк
```

| условие | баллы |
|---|---|
| distinct ≥ 2 или total ≥ 10 | 14 |
| total ≥ 3 | 8 |

Evidence: до 4 записей `{type: template, detail: "repeated template header '<строка>' ×N", line: первая строка}`.

Примечание: нумерованные разделы («1. …») намеренно НЕ оцениваются —
старые энциклопедии нумеруют значения слов (12–17 срабатываний на uk-файлах
human-корпуса).

### 2.2 Воздержание по жанру (v0.4.3, НЕ скоринговый сигнал)

Промо/соцсетевой регистр: `emoji >= 5` И `восклицания/слово >= 0.02` —
баллы НЕ начисляются (жанр дают и ИИ, и человеческие SMM-копирайтеры), но
вердикт дополняется пометкой: "NOTE: promotional/social-media register
(emoji- and exclamation-heavy) is outside the calibration corpus — this
verdict is unreliable for this genre." На корпусе калибровки не срабатывает
ни разу.

## 3. Связки (connective_density)

`CONNECTIVES` (~45, EN/RU/UK/PT, список в `parscgpt-ext.py`) считаются по
предложениям: `density = сумма вхождений связок / число предложений`.

| density ≥ | баллы |
|---|---|
| 0.12 | 13 |
| 0.08 | 7 |

## 4. Поддерживающие метрики (вес мал, преемственность v0.3.x)

| метрика | условие | баллы |
|---|---|---|
| lexical_diversity | < 0.45 / < 0.55 | 15 / 8 |
| entropy | < 5.0 / < 6.5 | 15 / 8 |
| pattern_repetition | > 0.35 | 10 |
| repetition | > 0.5 | 8 |
| punctuation_density | > 0.04 | 4 |
| unicode_symbols | непусто | 4 |
| avg_word_length | < 4.0 / < 4.5 | 5 / 3 |
| word_length_variance | < 1.5 | 4 |
| pronoun_ratio | > 0.15 | 4 |
| readability (Flesch) | > 70 / > 60 | 5 / 3 |
| passive_voice_density | > 0.05 | 4 |
| adj_noun_pair_diversity | < 0.3 | 3 |
| structural_uniformity | > 0.4 | 4 |
| quantifier_overuse | > 0.02 | 3 |

## 5. Итоговая формула

```
total  = сумма баллов всех метрик (максимум ~165, одиночная метрика ≤ 32)
lf     = min(1.0, words / 1000)
score  = min(100, round(total * (0.9 + 0.1 * lf), 1))
```

Интерпретация: > 70 — Strong AI-like; > 55 — Probable AI; > 35 — Mixed;
иначе — human-like. При `words < 150` или `sentences < 5` к вердикту
добавляется: "NOTE: text is too short for reliable structural analysis —
this verdict is unreliable, not evidence of human authorship."
Рекомендуемый порог классификации — 50–55 (см. отчёт валидации); 70 —
консервативный "strong AI".

## 6. Evidence (указатели на места)

Каждое сработавшее правило попадает в список evidence с привязкой к месту:

```
{type: phrase|uniformity|connective|sentence,
 detail: <описание>,
 line:   <номер строки 1-based или null>,
 excerpt: <цитата до ~110 символов с «>>>фраза<<<» или null>}
```

Порядок вывода: фразы HIGH → uniformity (предложения) → uniformity (абзацы)
→ MEDIUM-фразы → связки → топ-предложения по числу маркеров (≥2) → WEAK.
Лимит отображения — 15 записей.

Обязательные элементы:

1. **Фразы**: до 3 вхождений на фразу, до 10 в evidence; строка = позиция
   вхождения (подсчёт `\n` до offset + 1).
2. **Однородность предложений**: при sent_cv < 0.50 — CV + первые 25 длин.
3. **Однородность абзацев**: при para_cv < 0.45 — CV + длины абзацев.
4. **Связки**: при density ≥ 0.10 — до 2 предложений с ≥ 2 связками.
5. **Топ-предложения**: до 3 предложений с наибольшим числом маркеров
   (high=3, medium=2, weak=1, связка=1), порог ≥ 2.

## 7. Требования к портам

- Идентичные базы фраз/связок (копировать из `parscgpt-ext.py`).
- Идентичные пороги и веса из таблиц выше.
- `AI Probability` в stdout + evidence-секция в report-файл.
- Расхождение с референсом на любом файле корпуса — не более ±2 п.п.
